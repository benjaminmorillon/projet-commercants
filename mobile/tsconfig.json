{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // « jest » en plus des types d'Expo : sans lui, `describe` et `expect`
    // n'existent pas pour TypeScript et les fichiers de test ne compilent pas.
    "types": ["expo", "jest"]
  }
}
